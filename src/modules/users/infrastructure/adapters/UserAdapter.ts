/**
 * @deprecated Este archivo está deprecado.
 * Usa el adapter del dominio: import { UserAdapter } from '../../domain/adapters/UserAdapter'
 *
 * Los adapters son lógica de dominio (transformación de datos), no de infraestructura.
 * Este archivo se mantiene por compatibilidad pero será removido en el futuro.
 */

export { UserAdapter } from '../../domain/adapters/UserAdapter'
